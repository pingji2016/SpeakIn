# SpeakIn 流式语音识别改造方案

## 目录

1. [背景与目标](#1-背景与目标)
2. [当前架构分析](#2-当前架构分析)
3. [流式识别整体方案](#3-流式识别整体方案)
4. [详细设计](#4-详细设计)
5. [实施计划](#5-实施计划)
6. [风险与缓解](#6-风险与缓解)
7. [后续演进方向](#7-后续演进方向)

---

## 1. 背景与目标

### 1.1 当前问题

SpeakIn 目前的语音识别是**录完再识别**（Record-Then-Transcribe）模式：

```
用户按下录音 → AudioRecord 持续写 PCM 到文件
       ↓
用户按下停止 → 封装成 WAV 文件 → 整个文件送 ASR 引擎
       ↓
等待 3~10 秒 → 转写结果显示
```

**痛点**：
- 用户录音结束后需要**等待数秒**才能看到转写结果，体验割裂
- 录音过程中完全不知道转写质量（环境太吵？麦克风被挡？只能事后发现）
- 长录音的等待时间更长，焦虑感随录音时长递增

### 1.2 改造目标

实现**边录边识别**（Streaming ASR），达到以下效果：

| 指标 | 当前 | 目标 |
|------|------|------|
| 首次结果可见 | 录音结束 + 3~10s | 录音开始后 3~5s |
| 结果更新频率 | 仅一次 | 每 2~3s 刷新 |
| 录音结束后等待 | 3~10s（完整推理） | ~1s（仅增量推理） |
| 用户体验 | "黑盒等待" | "所见即所得" |

### 1.3 非目标

- 不做**逐字实时**级别的流式识别（需要替换为流式模型，如 SenseVoice / Paraformer）
- 不改变模型本身（继续使用 Whisper tiny）
- 不改变录音格式（16kHz / 16-bit / 单声道 PCM）

---

## 2. 当前架构分析

### 2.1 完整调用链

```
┌── UI 层 ──┐   RecordingBar.kt          [Compose]
│            │   点击开始/停止
├── VM 层 ──┤   NoteDetailViewModel.kt    [Jetpack ViewModel]
│            │   录音控制 + 转写调度
├── 领域层 ──┤   AudioRecorder.kt          [AudioRecord API]
│            │   AsrEngine.kt              [接口]
│            │   AsrEngineImpl.kt          [通过 AIDL 调用远程进程]
│            │   ModelServiceFacade.kt     [AIDL IPC 门面]
├── IPC 层 ──┤   IModelService.aidl        [Binder IPC]
│            │   IModelServiceCallback.aidl
├── 模型层 ──┤   ModelService.kt           [:model 独立进程]
│            │   ExecuTorchWhisperEngine.kt [Whisper tiny .pte]
│            │   WhisperTokenizer.kt        [BPE 解码]
└────────────┘
```

### 2.2 核心瓶颈

#### 瓶颈 1：AudioRecorder 不暴露中间数据

```kotlin
// AudioRecorder.kt — 当前实现
private fun writePcmData(outputFile: File, bufferSize: Int) {
    val buffer = ShortArray(bufferSize / 2)
    while (_isRecording) {
        val bytesRead = audioRecord?.read(buffer, 0, buffer.size) ?: -1
        if (bytesRead > 0) {
            // ShortArray → ByteArray → 写入文件
            // ← 这里没有把数据回调出去！
        }
    }
}
```

录音数据只写入文件，外部完全无法获取音频流。

#### 瓶颈 2：AsrEngine 接口只接受完整文件

```kotlin
// AsrEngine.kt — 当前接口
interface AsrEngine {
    fun transcribe(audioFile: File, callback: Callback)  // ← 只接受文件
}
```

没有流式输入的 API。

#### 瓶颈 3：ExecuTorchWhisperEngine 以整个 FloatArray 为单位

```kotlin
// ExecuTorchWhisperEngine.kt — 当前实现
fun transcribe(audioPcm: FloatArray, onProgress: ((Float) -> Unit)? = null): String? {
    val audioInput = if (audioPcm.size >= N_SAMPLES) audioPcm.copyOf(N_SAMPLES)
                     else FloatArray(N_SAMPLES).also { ... }  // 填充到 480000
    // Encoder → Decoder 自回归 → 返回完整文本
}
```

每次转写都是独立的、无状态的完整推理。

#### 瓶颈 4：AIDL 接口基于文件路径

```java
// IModelService.aidl
void transcribe(String audioPath, IModelServiceCallback callback);
```

音频通过文件路径传递，不适用于内存中的数据块。

---

## 3. 流式识别整体方案

### 3.1 方案选择

针对 Whisper 模型的特性（Encoder-Decoder 架构，30 秒输入窗口），我们采用 **"周期增量识别"** 策略：

```
录音过程中，每累积 N 秒新音频 → 触发一次增量识别 → 用最新结果更新 UI
```

这与真正的"逐帧流式"不同，但能在**不换模型**的前提下大幅改善体验。

#### 为什么不用真正的流式模型？

| 方案 | 优势 | 劣势 |
|------|------|------|
| **周期增量识别**（本方案） | 不换模型，改动可控，快速上线 | 有 2~3s 延迟，非逐字实时 |
| 替换为流式模型（SenseVoice 等） | 真正的实时，延迟 < 500ms | 需要新模型导出/集成/测试，周期长 |
| 使用 Android SpeechRecognizer | 系统内置，零模型体积 | 需联网，不可控，与"全离线"定位冲突 |

**选型结论**：先用周期增量识别快速上线改善体验，架构上预留流式模型接入点。

### 3.2 核心设计思路

```
录音开始
   │
   ▼
AudioRecorder 开始采集 ──→ 每 200ms 产生一个音频块 (FloatArray, 3200 samples)
   │                              │
   │                              ▼
   │                      AudioBuffer 累积
   │                              │
   │                    每 ~2s 触发一次识别
   │                              │
   │                              ▼
   │               ExecuTorchWhisperEngine
   │               对累积音频做完整推理
   │               (不足 30s 则补零)
   │                              │
   │                              ▼
   │               UI 更新 liveCaption
   │                              │
   │                     (继续累积，继续识别)
   │                              │
   ▼                              │
录音停止 ─────────────────────────┘
   │
   ▼
最后一次完整识别 → UI 显示最终结果
```

### 3.3 时序示意

```
时间轴:  0s    1s    2s    3s    4s    5s    6s    7s    8s
录音:    [████████████████████████████████████████████████] 停止
音频块:  [■][■][■][■][■][■][■][■][■][■][■][■][■][■][■][■]
识别#1:        ├─────┤              → 结果 "今天天气"      (2s 时的累积)
识别#2:              ├─────┤        → 结果 "今天天气很好"  (3.5s 时的累积)
识别#3:                    ├─────┤  → 结果 "今天天气很好适合出去玩" (5s 时)
最终:                                  ├─────────┤ → 最终结果 (停止后)
```

---

## 4. 详细设计

### 4.1 新增/改动的文件清单

| 文件 | 操作 | 说明 |
|------|------|------|
| `domain/audio/AudioRecorder.kt` | **改** | 新增 `AudioChunkListener` 回调 |
| `domain/audio/AudioBuffer.kt` | **新** | 音频数据累积器 |
| `domain/asr/AsrEngine.kt` | **改** | 新增流式 API |
| `domain/asr/StreamingAsrSession.kt` | **新** | 流式识别会话接口 |
| `domain/asr/AsrEngineImpl.kt` | **改** | 实现流式 API |
| `domain/service/ModelServiceFacade.kt` | **改** | 新增流式 IPC 方法 |
| `model-service/.../IModelService.aidl` | **改** | 新增流式 AIDL 方法 |
| `model-service/.../IStreamingAsrCallback.aidl` | **新** | 流式回调接口 |
| `model-service/.../ModelService.kt` | **改** | 处理流式转写请求 |
| `model-service/.../ExecuTorchWhisperEngine.kt` | **改** | 支持增量识别 |
| `ui/notedetail/NoteDetailViewModel.kt` | **改** | 集成流式识别与 live caption |
| `ui/recording/RecordingBar.kt` | **改** | 显示 live caption |

### 4.2 音频采集层改造

#### 4.2.1 AudioChunkListener — 音频块回调

```kotlin
// domain/audio/AudioRecorder.kt

class AudioRecorder {
    // 新增：音频块监听器
    interface AudioChunkListener {
        /**
         * 当有新音频数据可用时回调。
         * @param chunk PCM 数据，16kHz 16-bit 单声道，归一化到 [-1.0, 1.0]
         * @param timestampMs 自录音开始以来的时间戳
         */
        fun onAudioChunk(chunk: FloatArray, timestampMs: Long)
    }

    private var chunkListener: AudioChunkListener? = null

    fun setChunkListener(listener: AudioChunkListener?) {
        this.chunkListener = listener
    }

    // 修改 writePcmData 方法
    private fun writePcmData(outputFile: File, bufferSize: Int) {
        val buffer = ShortArray(bufferSize / 2)
        var totalSamples = 0L

        while (_isRecording) {
            val bytesRead = audioRecord?.read(buffer, 0, buffer.size) ?: -1
            if (bytesRead > 0) {
                // 1. 写入文件（保留原有逻辑，用于最终完整转写和播放）
                writeToFile(buffer, bytesRead, fileOutputStream)

                // 2. 转换为 FloatArray 并回调（新增）
                val floatChunk = FloatArray(bytesRead) { i ->
                    buffer[i].toFloat() / 32768f
                }
                totalSamples += bytesRead
                val timestampMs = totalSamples * 1000L / SAMPLE_RATE
                chunkListener?.onAudioChunk(floatChunk, timestampMs)
            }
        }
    }
}
```

#### 4.2.2 AudioBuffer — 音频累积器

```kotlin
// domain/audio/AudioBuffer.kt (新文件)

/**
 * 线程安全的音频数据累积器。
 * 用于流式识别场景：持续收集音频块，按需提供完整的 PCM 数组。
 */
class AudioBuffer(private val maxDurationSamples: Int = 480000) { // 默认 30s

    private val lock = Any()
    private val buffer = ArrayList<Float>()

    val durationMs: Long
        get() = synchronized(lock) { buffer.size * 1000L / 16000L }

    val sampleCount: Int
        get() = synchronized(lock) { buffer.size }

    /** 追加一个音频块 */
    fun append(chunk: FloatArray) {
        synchronized(lock) {
            for (sample in chunk) buffer.add(sample)
            // 超过最大长度时，保留最新的 N 个样本（滑窗）
            if (buffer.size > maxDurationSamples) {
                val excess = buffer.size - maxDurationSamples
                buffer.subList(0, excess).clear()
            }
        }
    }

    /** 获取当前累积的完整音频（不足 480000 时补零） */
    fun toFloatArray(targetSamples: Int = maxDurationSamples): FloatArray {
        synchronized(lock) {
            val result = FloatArray(targetSamples)
            val copySize = minOf(buffer.size, targetSamples)
            for (i in 0 until copySize) result[i] = buffer[i]
            return result
        }
    }

    /** 重置缓冲区（用于新的录音会话） */
    fun clear() {
        synchronized(lock) { buffer.clear() }
    }
}
```

### 4.3 ASR 引擎层改造

#### 4.3.1 StreamingAsrSession — 流式识别会话

```kotlin
// domain/asr/StreamingAsrSession.kt (新文件)

/**
 * 流式识别会话。
 *
 * 典型用法：
 * ```
 * val session = asrEngine.startStreaming(callback)
 * audioRecorder.setChunkListener { chunk, _ -> session.feedAudio(chunk) }
 * // ... 用户停止录音 ...
 * session.finish()
 * ```
 */
interface StreamingAsrSession {
    /** 喂入一个音频块 */
    fun feedAudio(chunk: FloatArray)

    /** 停止喂入，触发最后一次完整识别 */
    fun finish()

    /** 取消识别，释放资源 */
    fun cancel()

    /** 当前是否正在推理中 */
    val isRunning: Boolean
}
```

#### 4.3.2 AsrEngine 接口扩展

```kotlin
// domain/asr/AsrEngine.kt (修改后)

interface AsrEngine {

    // ─── 现有 API（保留，向后兼容） ───

    interface Callback {
        fun onResult(text: String)
        fun onProgress(progress: Float)
        fun onError(error: String)
    }

    /** 完整文件转写（保留用于离线/后台场景） */
    fun transcribe(audioFile: File, callback: Callback)

    fun isModelLoaded(): Boolean
    fun release()

    // ─── 新增：流式 API ───

    interface StreamingCallback {
        /** 部分识别结果（周期性更新，供 UI 展示 live caption） */
        fun onPartialResult(text: String)
        /** 最终识别结果（finish() 后的完整转写） */
        fun onFinalResult(text: String)
        /** 识别出错 */
        fun onError(error: String)
    }

    /**
     * 启动一个流式识别会话。
     * 调用方通过 [StreamingAsrSession.feedAudio] 持续喂入音频，
     * 引擎周期性触发识别并通过 [StreamingCallback.onPartialResult] 回传结果。
     */
    fun startStreaming(callback: StreamingCallback): StreamingAsrSession
}
```

#### 4.3.3 AsrEngineImpl 流式实现

```kotlin
// domain/asr/AsrEngineImpl.kt (新增部分)

@Singleton
class AsrEngineImpl @Inject constructor(
    private val modelService: ModelServiceFacade,
    private val asrModelManager: AsrModelManager
) : AsrEngine {

    // ... 现有 file-based transcribe 保留不变 ...

    override fun startStreaming(callback: AsrEngine.StreamingCallback): StreamingAsrSession {
        return StreamingAsrSessionImpl(modelService, asrModelManager, callback)
    }
}

/**
 * 流式识别会话实现。
 *
 * 核心策略：
 * 1. AudioBuffer 持续累积音频
 * 2. 每累积 ~2 秒新音频触发一次识别
 * 3. 如果上一次识别还在跑，跳过本次（避免堆积）
 * 4. finish() 时运行最后一次完整识别
 */
class StreamingAsrSessionImpl(
    private val modelService: ModelServiceFacade,
    private val asrModelManager: AsrModelManager,
    private val callback: AsrEngine.StreamingCallback
) : StreamingAsrSession {

    private val audioBuffer = AudioBuffer()
    private val recognitionIntervalMs = 2000L  // 每 2 秒触发一次识别
    private val minAudioForRecognition = 16000  // 至少 1 秒音频才开始识别

    private val scope = CoroutineScope(Dispatchers.IO + SupervisorJob())
    private var lastRecognitionTime = 0L
    private var running = AtomicBoolean(false)
    private var cancelled = AtomicBoolean(false)

    private val _isRunning = AtomicBoolean(false)
    override val isRunning: Boolean get() = _isRunning.get()

    override fun feedAudio(chunk: FloatArray) {
        if (cancelled.get()) return
        audioBuffer.append(chunk)

        val now = System.currentTimeMillis()
        val hasEnoughAudio = audioBuffer.sampleCount >= minAudioForRecognition
        val hasEnoughInterval = now - lastRecognitionTime >= recognitionIntervalMs
        val notAlreadyRunning = !running.get()

        if (hasEnoughAudio && hasEnoughInterval && notAlreadyRunning) {
            triggerRecognition(isFinal = false)
        }
    }

    override fun finish() {
        if (cancelled.get()) return

        // 取消可能正在跑的中间识别，以最终识别为准
        running.set(false)

        // 等待一小段确保线程安全，然后跑最终识别
        scope.launch {
            delay(100)
            triggerRecognition(isFinal = true)
        }
    }

    override fun cancel() {
        cancelled.set(true)
        running.set(false)
        audioBuffer.clear()
    }

    private fun triggerRecognition(isFinal: Boolean) {
        if (cancelled.get()) return
        running.set(true)
        _isRunning.set(true)
        lastRecognitionTime = System.currentTimeMillis()

        scope.launch {
            try {
                // 取当前累积音频的快照
                val audioData = audioBuffer.toFloatArray()
                if (!asrModelManager.isModelReady()) {
                    callback.onError("ASR 模型未就绪")
                    return@launch
                }

                modelService.bind()
                modelService.loadAsr(asrModelManager.getModelDir())

                val result = modelService.transcribeAudioData(audioData)

                result.fold(
                    onSuccess = { text ->
                        if (isFinal) {
                            callback.onFinalResult(text)
                        } else {
                            callback.onPartialResult(text)
                        }
                    },
                    onFailure = { e ->
                        callback.onError(e.message ?: "转写失败")
                    }
                )
            } catch (e: Exception) {
                if (!cancelled.get()) {
                    callback.onError("转写出错: ${e.message}")
                }
            } finally {
                running.set(false)
                _isRunning.set(false)
            }
        }
    }
}
```

### 4.4 IPC 层改造

#### 4.4.1 AIDL 接口新增

```java
// IModelService.aidl (新增方法)

interface IModelService {
    // ... 现有方法保留不变 ...

    /**
     * 流式转写：直接接收 PCM FloatArray 数据。
     * 这是为流式识别场景设计的低延迟方法，
     * 跳过了文件 I/O 和 WAV 解析步骤。
     */
    void transcribePcm(in float[] pcmData, IModelServiceCallback callback);

    /**
     * 取消正在进行的转写。
     */
    void cancelTranscribe();
}
```

#### 4.4.2 ModelServiceFacade 新增

```kotlin
// domain/service/ModelServiceFacade.kt (新增方法)

/**
 * 直接转写 PCM 数据（流式场景，跳过文件 I/O）。
 */
suspend fun transcribeAudioData(pcmData: FloatArray): Result<String> {
    val svc = awaitService() ?: return Result.failure(Exception("服务未连接"))

    return suspendCancellableCoroutine { cont ->
        val callback = object : IModelServiceCallback.Stub() {
            override fun onResult(text: String) {
                cont.resume(Result.success(text))
            }
            override fun onError(error: String) {
                if (cont.isActive) cont.resume(Result.failure(Exception(error)))
            }
            override fun onProgress(progress: Float) { /* 流式场景不需要 */ }
        }
        svc.transcribePcm(pcmData, callback)
    }
}

/**
 * 取消当前转写。
 */
fun cancelTranscribe() {
    try { service?.cancelTranscribe() } catch (_: Exception) {}
}
```

#### 4.4.3 ModelService 适配

```kotlin
// model-service/ModelService.kt (新增方法)

private val binder = object : IModelService.Stub() {
    // ... 现有方法 ...

    override fun transcribePcm(pcmData: FloatArray, callback: IModelServiceCallback?) {
        val cb = callback ?: return
        // 取消之前的转写（如果有）
        currentJob?.cancel()

        currentJob = workerHandler?.post {
            try {
                val result = asrEngine.transcribe(pcmData) { progress ->
                    cb.onProgress(progress)
                }
                if (result != null) cb.onResult(result)
                else cb.onError("转写失败")
            } catch (e: Exception) {
                cb.onError(e.message ?: "未知错误")
            }
        }
    }

    override fun cancelTranscribe() {
        currentJob?.cancel()
    }
}
```

### 4.5 UI 层改造

#### 4.5.1 NoteDetailViewModel 流程改造

```kotlin
// NoteDetailViewModel.kt (核心改动)

// 新增 State
data class NoteDetailUiState(
    // ... 现有字段 ...
    val liveCaption: String = "",        // 新增：实时字幕
    val isStreamingRecognition: Boolean = false,  // 新增：流式识别进行中
)

// 新增成员
private var streamingSession: StreamingAsrSession? = null
private var accumulatedFinalText: String = ""

fun startRecording() {
    val fileName = "rec_${UUID.randomUUID().toString()}.m4a"
    val file = File(audioOutputDir, fileName)
    val started = audioRecorder.start(file)
    if (started) {
        currentAudioFile = file
        _uiState.value = _uiState.value.copy(
            isRecording = true,
            liveCaption = ""  // 清空上轮字幕
        )

        // ─── 新增：启动流式识别 ───
        startStreamingRecognition()
    }
}

private fun startStreamingRecognition() {
    val session = asrEngine.startStreaming(object : AsrEngine.StreamingCallback {
        override fun onPartialResult(text: String) {
            _uiState.value = _uiState.value.copy(
                liveCaption = text,
                isStreamingRecognition = true
            )
        }

        override fun onFinalResult(text: String) {
            accumulatedFinalText = text
            _uiState.value = _uiState.value.copy(
                liveCaption = text,
                isStreamingRecognition = false
            )
        }

        override fun onError(error: String) {
            // 流式识别失败不中断录音，用户仍可在停止后获得完整转写
            Log.w(TAG, "Streaming recognition error: $error")
        }
    })

    streamingSession = session

    // 将 AudioRecorder 的音频块连接到 ASR 会话
    audioRecorder.setChunkListener { chunk, _ ->
        session.feedAudio(chunk)
    }
}

fun stopRecording() {
    val durationMs = audioRecorder.stop()
    // 停止音频回调
    audioRecorder.setChunkListener(null)

    val file = currentAudioFile
    if (file == null || durationMs <= 0) {
        _uiState.value = _uiState.value.copy(isRecording = false)
        return
    }

    _uiState.value = _uiState.value.copy(isRecording = false, isTranscribing = true)

    // ─── 先结束流式会话，获取最终结果 ───
    streamingSession?.finish()

    viewModelScope.launch {
        // 等待流式识别最终结果（最多 5 秒）
        var finalText = accumulatedFinalText
        if (finalText.isBlank()) {
            // 流式识别未产生结果，回退到文件转写
            withTimeoutOrNull(5000) {
                // 等待 accumulatedFinalText 被填充
                while (accumulatedFinalText.isBlank() && _uiState.value.isStreamingRecognition) {
                    delay(200)
                }
                finalText = accumulatedFinalText
            }
        }

        // 如果流式识别完全失败，回退到完整文件转写
        if (finalText.isBlank()) {
            finalText = transcribeFileFallback(file)
        }

        // 后续的润色逻辑不变
        if (finalText.isNotBlank()) {
            val block = repository.addVoiceBlock(noteId, file, durationMs)
            repository.updateTranscription(block.id, finalText)
            // Polish...
        }

        _uiState.value = _uiState.value.copy(
            isTranscribing = false,
            liveCaption = ""
        )
    }
}

// 回退方案：完整文件转写
private suspend fun transcribeFileFallback(file: File): String {
    return withContext(Dispatchers.IO) {
        val signal = CompletableDeferred<String>()
        asrEngine.transcribe(file, object : AsrEngine.Callback {
            override fun onResult(text: String) { signal.complete(text) }
            override fun onProgress(progress: Float) {}
            override fun onError(error: String) { signal.complete("") }
        })
        signal.await()
    }
}
```

#### 4.5.2 RecordingBar UI 改造

```kotlin
// RecordingBar.kt (新增 live caption 区域)

@Composable
fun RecordingBar(
    isRecording: Boolean,
    liveCaption: String,          // 新增：实时字幕
    isStreamingRecognition: Boolean, // 新增：是否正在流式识别
    onStartRecording: () -> Unit,
    onStopRecording: () -> Unit,
    modifier: Modifier = Modifier
) {
    // ... 现有 UI ...

    // 新增：实时字幕显示区域
    if (isRecording && liveCaption.isNotEmpty()) {
        Spacer(modifier = Modifier.height(12.dp))
        Card(
            modifier = Modifier.fillMaxWidth(),
            shape = RoundedCornerShape(12.dp),
            colors = CardDefaults.cardColors(
                containerColor = MaterialTheme.colorScheme.surfaceVariant
            )
        ) {
            Row(
                modifier = Modifier.padding(12.dp),
                verticalAlignment = Alignment.Top
            ) {
                // 流式识别指示器
                if (isStreamingRecognition) {
                    CircularProgressIndicator(
                        modifier = Modifier.size(14.dp),
                        strokeWidth = 2.dp
                    )
                    Spacer(modifier = Modifier.width(8.dp))
                }
                Text(
                    text = liveCaption,
                    style = MaterialTheme.typography.bodyMedium,
                    color = MaterialTheme.colorScheme.onSurfaceVariant
                )
            }
        }
    }
}
```

### 4.6 关键设计决策

#### 4.6.1 识别触发间隔：2 秒

| 间隔 | 延迟感 | CPU 占用 | 选择 |
|------|--------|---------|------|
| 500ms | 低 | 高（识别跑不完就堆积） | ❌ |
| **2s** | **可接受** | **适中** | ✅ |
| 5s | 明显等待 | 低 | ❌ |

Whisper tiny 在手机上推理一次需要 2~4 秒。2 秒的触发间隔配合"跳过正在运行的识别"策略，实际触发间隔在 2~4 秒之间，用户感知为"字幕每几秒刷新一次"。

#### 4.6.2 最小识别音频量：1 秒

少于 1 秒的音频信息量太少，Whisper 容易产生幻觉输出。设置 16000 samples（1 秒）为最小阈值。

#### 4.6.3 并发策略：跳过而非排队

当上一次识别还在运行时，新的音频块到达不排队新的识别任务，而是直接跳过。理由：
- Whisper 推理是 CPU 密集型，并行推理会让手机发烫且两个推理都变慢
- 排队会导致结果延迟越来越长（"雪崩效应"）
- 跳过的下一次触发会拿到更多累积音频，结果质量更好

#### 4.6.4 finish() 策略：取最新 + 回退

```
finish() 调用时：
  1. 如果有流式识别正在运行 → 等待它完成 → 取其结果为 final
  2. 如果没有 → 立即触发一次完整识别 → 取其结果为 final
  3. 如果流式结果为空/失败 → 回退到完整文件转写（现有路径）
```

#### 4.6.5 音频数据传输优化

流式场景中，音频数据不再通过文件路径传递，而是直接在 AIDL 中传递 FloatArray：
- 省去文件 I/O 的写入+读取
- 省去 WAV 格式的编解码
- 2 秒音频在 AIDL 中传输量：32000 floats × 4 bytes = 128 KB，Binder 完全承受得住

### 4.7 线程模型

```
主线程 (UI Thread)
  │
  ├─ AudioRecord.read()        [独立录制线程]
  │     └─→ AudioChunkListener.onAudioChunk()
  │           └─→ StreamingAsrSession.feedAudio()  [线程安全，仅追加到 buffer]
  │
  ├─ 周期性触发识别             [CoroutineScope(IO)]
  │     └─→ AIDL IPC
  │           └─→ ModelService  [HandlerThread "model-worker"]
  │                 └─→ ExecuTorchWhisperEngine.transcribe()
  │
  └─ 结果回调                  [main-safe]
        └─→ NoteDetailViewModel._uiState.update()
```

---

## 5. 实施计划

### 5.1 分阶段实施

#### Phase 1：音频采集层（1 天）

- [ ] `AudioBuffer` 实现与单元测试
- [ ] `AudioRecorder` 新增 `AudioChunkListener`
- [ ] 验证：打印 chunk 大小和时间戳，确认数据正确

#### Phase 2：引擎层 + IPC 层（2 天）

- [ ] AIDL 新增 `transcribePcm` / `cancelTranscribe`
- [ ] `ModelService` 适配 PCM 输入
- [ ] `ModelServiceFacade` 新增 `transcribeAudioData`
- [ ] `StreamingAsrSessionImpl` 实现
- [ ] 验证：用固定音频数据测试端到端流式转写

#### Phase 3：UI 层（1.5 天）

- [ ] `NoteDetailViewModel` 集成流式识别
- [ ] `RecordingBar` 新增 live caption 区域
- [ ] 状态管理（录音中/识别中/完成/错误）
- [ ] 验证：完整录音→识别→显示流程

#### Phase 4：打磨（1 天）

- [ ] 降级/回退测试（流式失败 → 文件转写）
- [ ] 长录音测试（> 30 秒）
- [ ] 性能测试（CPU/内存/发热）
- [ ] 边界情况（空录音、极短录音、快速开始停止）

### 5.2 总预估工期：5~6 天

---

## 6. 风险与缓解

| 风险 | 概率 | 影响 | 缓解措施 |
|------|------|------|---------|
| **Whisper tiny 推理太慢**，导致流式结果滞后严重 | 中 | 用户体验差 | 先用 tiny 模型（最快）；后续考虑换 base 模型或量化优化 |
| **AIDL 传大 FloatArray 触发 TransactionTooLarge** | 低 | 流式识别崩溃 | 2s 音频 = 128KB，远低于 1MB Binder 限制；30s 音频 = 1.9MB，改用共享内存 |
| **频繁推理导致手机发热/耗电** | 中 | 用户投诉 | 限制最大识别频率（2s 间隔 + 跳过排队）；监控 CPU 温度 |
| **部分识别结果质量差**（短音频上下文不足） | 中 | 用户对 live caption 失去信任 | UI 上标注"实时预览"；录音结束后用完整音频做最终转写替换 |
| **与现有文件转写路径冲突** | 低 | 功能回归 | 保留完整的旧路径作为回退方案；充分回归测试 |

---

## 7. 后续演进方向

### 7.1 短期（v1.1）：VAD 语音活动检测

在 AudioBuffer 前端加入 VAD：
- 只对有语音的片段触发识别
- 自动切分句子（基于静音间隔）
- 减少无效推理，降低功耗

### 7.2 中期（v1.2）：Encoder 输出缓存

```
第一次识别: [音频 0-2s 补零到 30s] → Encoder → Decoder → 文本
第二次识别: [音频 0-3s 补零到 30s] → Encoder → Decoder → 文本
                                       ↑ 前面 2s 的编码重复计算了！
```

优化方案：
- 缓存已编码的音频段的 encoder output
- 只编码新增部分，拼接 encoder outputs
- 注：Whisper 使用绝对位置编码，拼接需谨慎处理（分段编码 + 位置偏移）

### 7.3 长期（v2.0）：替换为真正的流式模型

当需要**逐字实时**级别的体验时，替换为流式 ASR 模型：

| 候选模型 | 推理框架 | 模型大小 | 流式能力 |
|---------|---------|---------|---------|
| SenseVoice-Small | ONNX Runtime | ~80MB | ✅ 原生流式 |
| Paraformer-large | FunASR / ONNX | ~200MB | ✅ 原生流式 |
| Whisper-streaming | ExecuTorch | ~75MB | ✅ 需要 adapter |

依托 `AsrEngine` 接口的抽象，替换模型只需：
1. 实现新的 `AsrEngine`（如 `SenseVoiceEngine`）
2. 修改 DI 绑定
3. UI 层零改动

---

## 附录 A：接口变更汇总

```diff
# AsrEngine.kt
+ interface StreamingCallback {
+     fun onPartialResult(text: String)
+     fun onFinalResult(text: String)
+     fun onError(error: String)
+ }
+ fun startStreaming(callback: StreamingCallback): StreamingAsrSession

# AudioRecorder.kt
+ interface AudioChunkListener {
+     fun onAudioChunk(chunk: FloatArray, timestampMs: Long)
+ }
+ fun setChunkListener(listener: AudioChunkListener?)

# IModelService.aidl
+ void transcribePcm(in float[] pcmData, IModelServiceCallback callback);
+ void cancelTranscribe();

# NoteDetailUiState
+ val liveCaption: String
+ val isStreamingRecognition: Boolean
```

## 附录 B：新增文件清单

```
app/src/main/java/com/speakin/app/
├── domain/audio/
│   └── AudioBuffer.kt                    [新] 音频累积器
├── domain/asr/
│   └── StreamingAsrSession.kt            [新] 流式识别会话接口

model-service/src/main/java/com/speakin/modelservice/
（AIDL 修改 + ModelService 修改，无完全新文件）
```

---

> **文档版本**: v1.0  
> **最后更新**: 2026-06-20  
> **作者**: SpeakIn 技术团队
